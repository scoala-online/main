// import {FooterHeight, minFooterHeight} from '../../themes/Sizes';

const footerStyles = {
    footer: {
        width: '100vw',
        position: 'absolute',
        bottom: '0',
    },
};

export default footerStyles;
