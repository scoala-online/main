import styles from './FooterSecondFragmentStyles';

export default function FooterSecondFragment(props) {
    const dimensions = props.dimensions;

    return (
        <div style={styles.FooterSecondFragment}>
            
        </div>
    )
}